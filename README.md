# Snake
A simple snake game using python and MPI to showcase multi-process interactions to highlight specific commands


## Requirements
- MPI (both games)
'python -m pip install mpi4py'
- pygame (UI game)
'python -m pip install pygame'
